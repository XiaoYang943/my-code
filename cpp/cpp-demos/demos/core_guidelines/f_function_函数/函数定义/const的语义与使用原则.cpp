//
// Created by Administrator on 2026/2/14.
//

/*
================================================================================
主题：const 的语义与工程使用原则
================================================================================

一、const 的核心含义

    const 表示“不可修改的约束”。
    它不是为了限制程序员，而是为了表达接口契约。

    本质上：
        const 是类型系统的一部分，
        用于在编译期保证对象不会被意外修改。

--------------------------------------------------------------------------------
二、const 在函数接口中的意义

    1. 通过 const 引用传参

        void foo(const std::string& str);

        表示：
            - 函数不会修改 str
            - 避免拷贝开销
            - 提升接口安全性

    2. 通过 const 指针传参

        void foo(const int* ptr);

        表示：
            - 不能通过 ptr 修改其指向的值

--------------------------------------------------------------------------------
三、工程级原则

    1. 只要变量不需要被修改，就应该加 const
        → 提高可读性
        → 减少误操作
        → 让编译器帮助检查

    2. 函数参数优先使用 const 引用
        → 避免不必要的拷贝
        → 明确接口契约

    3. const 是设计工具，而不是语法装饰
        → 在接口层表达不变性
        → 在实现层增强安全性

--------------------------------------------------------------------------------
核心思想：

    const 表达的是“不会被修改”的承诺，
    是接口设计的重要组成部分，
    而不仅仅是语法层面的限制。

================================================================================
*/

int main() {

}
