//
// Created by Administrator on 2026/2/14.
//

/*
================================================================================
主题：auto 使用原则与工程规范
================================================================================

一、总体原则

    当变量类型可以从初始化表达式中清晰推导，且不会影响代码可读性时，
    优先使用 auto。

    auto 的本质是：
        让编译器推导类型，而不是让程序员重复书写类型。

--------------------------------------------------------------------------------
二、推荐使用 auto 的场景

    1. 泛型编程（模板、STL 容器迭代器）
        - 避免冗长类型名称
        - 减少类型重复书写
        - 降低重构成本

    2. 复杂类型表达式
        例如：
            std::unordered_map<std::string, std::vector<int>>::iterator

        使用 auto 可提升可读性。

    3. 明显的初始化表达式
        例如：
            auto count = 10;
            auto ptr = std::make_unique<Foo>();

--------------------------------------------------------------------------------
三、不推荐使用 auto 的场景

    1. 变量作用域较大（如函数外部、长函数内部核心变量）
        - 读者需要一眼明确其类型

    2. 初始化表达式类型不明显
        - 可能导致误解
        - 特别是涉及整数类型推导（int / long / size_t）

    3. 需要明确控制精度或范围
        - 例如 float / double
        - int / long long
        - 有符号 / 无符号

--------------------------------------------------------------------------------
四、工程级建议

    - auto 是“减少重复”的工具，而不是“隐藏类型”的工具
    - 类型是否写出来，取决于是否影响阅读理解
    - 在接口层（API 边界）尽量显式类型
    - 在局部实现细节中合理使用 auto

核心思想：

    类型信息是接口契约的一部分。
    在需要表达约束时显式，在实现细节中简化。

================================================================================
*/
int main() {

}
