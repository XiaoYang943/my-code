//
// Created by Administrator on 2026/2/14.
//

/*
================================================================================
C++ Core Guidelines — ES.45

避免“魔法常量”（magic constants）；使用具名常量

--------------------------------------------------------------------------------
原则说明：

    不要在表达式中直接写裸露的数值常量，
    应使用具有语义的命名常量代替。

--------------------------------------------------------------------------------
原因：

    1. 嵌入表达式中的未命名常量容易被忽略
    2. 读者难以理解其含义
    3. 修改困难（需要全局搜索替换）
    4. 容易造成语义错误

--------------------------------------------------------------------------------
什么是“魔法常量”？

    例如：

        if (timeout > 300) { ... }
        buffer.resize(1024);
        if (status == 2) { ... }

    这些数字的含义并不清晰：
        300 是秒？毫秒？
        1024 是最大长度？默认大小？
        2 代表什么状态？

--------------------------------------------------------------------------------
正确做法：

    使用具名常量表达语义：

        constexpr int MaxBufferSize = 1024;
        constexpr int TimeoutSeconds = 300;

        enum class Status {
            Ready = 0,
            Running = 1,
            Error = 2
        };

--------------------------------------------------------------------------------
工程级理解：

    数字本身没有语义，
    名称才有语义。

    代码应该表达“意图”，
    而不是暴露实现细节。

--------------------------------------------------------------------------------
核心思想：

    用名字表达含义，
    用类型表达约束。

================================================================================
*/
